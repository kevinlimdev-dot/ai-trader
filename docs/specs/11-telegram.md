# 11 - 텔레그램 연동

## 1. 개요

AI Trader는 **OpenClaw Gateway의 텔레그램 채널**을 통해 봇을 제어할 수 있다. 대시보드의 모든 주요 기능을 텔레그램 DM으로 동일하게 사용할 수 있으며, 시스템 상태 알림도 자동으로 수신한다.

### 핵심 특징

- **자연어 명령**: "자동매매 시작", "잔고", "포지션" 등 한국어/영어 자연어로 명령
- **대시보드 동일 기능**: 텔레그램 명령 = 대시보드 버튼 클릭과 동일한 동작
- **프로액티브 알림**: Kill Switch, 큰 손실, Runner/Monitor 이상 시 자동 텔레그램 알림
- **Block Mode 전달**: 완성된 응답을 한 번에 전달 (스트리밍 미적용, 안정적)

---

## 2. 설정

### 2.1 OpenClaw 텔레그램 채널 설정

```bash
# 텔레그램 채널 활성화
openclaw config set channels.telegram.enabled true

# DM 정책: pairing (페어링된 사용자만)
openclaw config set channels.telegram.dmPolicy pairing

# 스트림 모드: block (완성 후 전달, 긴 응답도 안정적)
openclaw config set channels.telegram.streamMode block

# 데몬 재시작으로 적용
openclaw daemon restart
```

### 2.2 텔레그램 봇 페어링

```bash
# 페어링 요청 승인
openclaw pairing approve telegram
```

사용자가 텔레그램에서 `@aiiiiitrading_bot`에게 DM을 보내면, OpenClaw Gateway가 페어링 요청을 생성한다. 위 명령으로 승인하면 DM 통신이 활성화된다.

### 2.3 설정 값 정리

| 설정 키 | 값 | 설명 |
|---------|-----|------|
| `channels.telegram.enabled` | `true` | 텔레그램 채널 활성화 |
| `channels.telegram.dmPolicy` | `pairing` | 페어링된 사용자만 DM 가능 |
| `channels.telegram.streamMode` | `block` | 완성 후 한번에 전달 |

> **streamMode 옵션:** `off` (전달 안 함), `partial` (점진적 전달, 긴 응답 시 불안정), `block` (권장, 완성 후 전달)

---

## 3. 명령어 매핑

텔레그램에서 자연어로 명령하면, `ai-trader` 스킬의 SKILL.md에 정의된 매핑에 따라 적절한 스크립트가 실행된다.

### 3.1 트레이딩 제어

| 텔레그램 명령 | 동작 | 실행 스크립트/API |
|-------------|------|-----------------|
| "자동매매 시작" / "start trading" | Runner 연속 실행 시작 | `curl POST /api/bot/runner {action:"start"}` |
| "자동매매 정지" / "stop trading" | Runner 정지 | `curl POST /api/bot/runner {action:"stop"}` |
| "1회 실행" / "run once" | 파이프라인 1회 실행 | `curl POST /api/bot/runner {action:"once"}` |
| "긴급 청산" / "emergency" | Kill Switch + 전량 청산 | `bun run execute-trade.ts --action emergency` |

### 3.2 정보 조회

| 텔레그램 명령 | 동작 | 실행 스크립트 |
|-------------|------|-------------|
| "잔고" / "balance" | HL + Coinbase 잔고 표시 | `bun run manage-wallet.ts --action balance` |
| "포지션" / "positions" | 열린 포지션 목록 | `bun run execute-trade.ts --action positions` |
| "일일요약" / "daily" | 오늘의 거래 요약 | `bun run execute-trade.ts --action daily-summary` |
| "최근 로그" / "logs" | Runner 로그 최근 50줄 | `tail -50 data/runner.log` |
| "상태" / "status" | 시스템 전체 상태 | Runner + Monitor 상태 파일 읽기 |

### 3.3 전략 관리

| 텔레그램 명령 | 동작 | 실행 방법 |
|-------------|------|----------|
| "전략 변경 aggressive" | 전략 프리셋 변경 | `curl POST /api/bot/strategy` |
| "현재 전략" / "strategy" | 현재 전략 조회 | `curl GET /api/bot/strategy` |

### 3.4 시장 데이터

| 텔레그램 명령 | 동작 | 실행 스크립트 |
|-------------|------|-------------|
| "시장 분석" / "market" | 최신 시그널 분석 결과 | `bun run analyze.ts` 결과 읽기 |
| "시장 심리" / "sentiment" | 시장 심리 요약 | `bun run collect-sentiment.ts` 결과 읽기 |

---

## 4. 응답 형식

### 4.1 잔고 조회 응답 예시

```
💰 AI Trader 잔고 현황

HyperLiquid: $939.18 USDC (Arbitrum)
Coinbase: $0.00 USDC (Base)
합계: $939.18

포지션: 5개 / 15개 (여유 10슬롯)
일일 PnL: +$12.34 (+1.3%)
```

### 4.2 자동매매 시작 응답 예시

```
🚀 자동매매가 시작되었습니다.

모드: LIVE
전략: Aggressive
간격: 5분
OpenClaw: 연결됨

다음 사이클: 5분 후
```

### 4.3 AI 판단 결과 보고 예시

```
📊 AI Trader 파이프라인 완료

가격 수집: ✅ 50개 코인
기술 분석: ✅ 8 시그널
시장 심리: ✅ bullish 15, bearish 12, neutral 23
AI 판단: ✅

승인: BTC LONG (RSI 반등 + 스마트머니 롱 + 군중 숏)
승인: ZEC SHORT (MACD 데드크로스 + 펀딩비 높음)
거부: ETH LONG (군중+스마트머니 모두 롱 → 과열)

거래 실행: 2건 체결, 1건 거부
```

---

## 5. 프로액티브 알림

OpenClaw의 `HEARTBEAT.md` 설정에 따라 ~30분 간격으로 자동 체크하고, 이상 시 텔레그램으로 알린다.

### 5.1 알림 트리거

| 상황 | 알림 내용 | 우선순위 |
|------|---------|---------|
| Kill Switch 활성화 | "⛔ Kill Switch 활성화됨. 모든 거래 중단." | 즉시 |
| Runner 에러 상태 | "⚠ Runner가 에러 상태입니다. 확인 필요." | 즉시 |
| 일일 손실 > 10% | "🔴 일일 손실 10% 초과. 주의 필요." | 즉시 |
| 포지션 모니터 중지 | "⚠ Position Monitor가 중지되었습니다." | 즉시 |
| Runner 예기치 않은 중지 | "⚠ Runner가 예기치 않게 중지되었습니다." | 즉시 |

### 5.2 알림 규칙

- **이상 없으면**: 알림 안 보냄 (silence)
- **반복 방지**: 동일 이슈에 대해 반복 알림 금지
- **야간 시간**: 00:00~07:00 KST에는 긴급 알림만 발송
- **일일 리포트**: 매일 22:00 KST에 자동 일일 요약 (cron 설정 시)

---

## 6. streamMode 선택 가이드

| 모드 | 동작 | 장점 | 단점 |
|------|------|------|------|
| `off` | 텔레그램 전달 안 함 | — | 알림 불가 |
| `partial` | 점진적 스트리밍 | 빠른 응답 시작 | 긴 응답 시 Telegram 4096자 제한 문제 |
| **`block`** (권장) | 완성 후 한번에 전달 | 안정적, 자동 분할 | 응답 완성까지 대기 |

> **현재 설정**: `block` — 긴 로그나 분석 결과도 안정적으로 전달됨

---

## 7. 트러블슈팅

### 7.1 텔레그램에 응답이 안 보일 때

1. OpenClaw 대시보드(`http://127.0.0.1:18789`)에서 메시지 확인
2. `openclaw config get channels.telegram.streamMode` → `block` 확인
3. `openclaw daemon restart` 후 재시도

### 7.2 "Delivering to Telegram requires target <chatId>" 에러

- CLI에서 `--deliver`로 직접 전달 시 발생
- 텔레그램 DM으로 직접 보내면 정상 동작 (chatId 자동 인식)

### 7.3 페어링 실패

```bash
# 페어링 상태 확인
openclaw pairing list

# 재페어링
openclaw pairing approve telegram
```

---

## 관련 문서

- [06-config-and-deployment.md](./06-config-and-deployment.md) — OpenClaw 설정
- [01-overview.md](./01-overview.md) — 시스템 아키텍처
- [10-ai-decision.md](./10-ai-decision.md) — AI 자율 투자 판단
